@model SAD.Cobranca.UI.Models.RegisterViewModel
@{
    ViewBag.Title = "Registrar | SAD";
}
<div class="container-d">
    <div class="container">
        <div class="row pt-5">
            <hgroup class="mb-3">
                <h1 class="col-12 font-weight-bold px-0">REGISTRAR NOVO USUÁRIO</h1>
                <h5 class="col-12 font-weight-light px-0">Preencha os campos abaixo para registrar novo usuário</h5>
            </hgroup>
        </div>
        @Html.Partial("_MenuUsuarios")
        <div class="row justify-content-lg-start">
            <div class="col-lg-6 col-sm-12 px-0">
                @using (Html.BeginForm("Register", "Account", FormMethod.Post, new { @class = "", role = "form" }))
                {
                    @Html.AntiForgeryToken()
                    @Html.ValidationSummary("", new { @class = "text-danger" })

                    <div class="row form-group">
                        <div class="col-lg-12">
                            @Html.LabelFor(m => m.Email, new { @class = "col-lg-12 px-0 col-form-label text-uppercase f-size-6" })
                            @Html.TextBoxFor(m => m.Email, new { @class = "form-control form-control-lg" })
                        </div>
                    </div>

                    <div class="row form-group">
                        <div class="col-lg-12">
                            @Html.LabelFor(m => m.Password, new { @class = "col-lg-12 px-0 col-form-label text-uppercase f-size-6" })
                            @Html.PasswordFor(m => m.Password, new { @class = "form-control form-control-lg" })
                        </div>
                    </div>

                    <div class="row form-group">
                        <div class="col-lg-12">
                            @Html.LabelFor(m => m.ConfirmPassword, new { @class = "col-lg-12 px-0 col-form-label text-uppercase f-size-6" })
                            @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control form-control-lg" })
                        </div>
                    </div>

                    <div class="row form-group">
                        <div class="col-lg-12">
                            @Html.ActionLink("Voltar", null, null, null, new { href = Request.UrlReferrer, @class = "btn btn-link" })
                            <input type="submit" class="btn btn-primary cursor-p" value="Registrar" />
                        </div>
                    </div>
                }
            </div>
            <div class="col-lg-6 col-sm-12 px-0">
                @Html.Partial("_ErrorSummary")
            </div>
        </div>
    </div>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}